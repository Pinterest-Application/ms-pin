<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="create-comment-table" author="pin_user">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="comment"/>
            </not>
        </preConditions>

        <createTable tableName="comment">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" primaryKeyName="pk_comment_id"/>
            </column>

            <column name="pin_id" type="uuid">
                <constraints nullable="false"
                             foreignKeyName="fk_comment_pin_id"
                             references="pin(id)"
                             deleteCascade="true"/>
            </column>

            <column name="user_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="content" type="varchar(1000)">
                <constraints nullable="false"/>
            </column>

            <column name="is_active" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="timestamp with time zone" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_comment_pin_id" tableName="comment">
            <column name="pin_id"/>
        </createIndex>

        <createIndex indexName="idx_comment_user_id" tableName="comment">
            <column name="user_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="comment"/>
        </rollback>
    </changeSet>

</databaseChangeLog>